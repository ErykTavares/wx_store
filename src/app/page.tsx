'use client';

import DefaultLayout from '@/layout/defaultLayout';
import React from 'react';

const Home = (): JSX.Element => (
	<DefaultLayout>
		<div>test</div>
	</DefaultLayout>
);
export default Home;
